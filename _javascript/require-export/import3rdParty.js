var _ = require('lodash');
var uuid = require('uuid');

console.log('Module started');

console.log(uuid()); //will generate a new guid()
console.log(uuid()); //will generate a new guid()
console.log(uuid()); //will generate a new guid()
